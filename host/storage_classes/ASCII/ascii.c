#include <stdio.h>

int main()
{
    // stores the ASCII value of respected characters
    char A = 'A';
    char n = 'n';
    char d = 'd';

    // string is printed using %c
    printf("The string is %c%c%c\n",A,n,d);

    // ASCII value of characters initialized
    printf("the ASCII value of And is %d %d %d",A,n,d);
}